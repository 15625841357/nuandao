<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.apache.org//DTD Mapper 3.0//EN"
        "http://ibatis.apache.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.support.mapper.community_userMapper">

    <select id="findAllByCommunity_id" resultType="community_user">
        select * from community_user c where c.community_id = #{id}
    </select>

</mapper>